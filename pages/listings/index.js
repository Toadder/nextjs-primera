import Head from 'next/head';
import ListingsPage from '@/src/pages/ListingsPage';

const Listings = () => (
	<>
		<Head>
			<title>Primera | Listings</title>
		</Head>
		<ListingsPage />
	</>
);

export default Listings;
